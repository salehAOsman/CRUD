@model CRUD.Models.Person


@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<table class="table">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Phone</th>
        <th>City</th>
    </tr>
        <tr>
            <td>@Model.Id</td>
            <td>@Model.Name</td>
            <td>@Model.Phone</td>
            <td>@Model.City</td>
        </tr>

</table>

@Html.ActionLink("Back home", "Index")
